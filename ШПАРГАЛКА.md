# 📝 Шпаргалка - Base Mini App

## 🚀 Создание нового проекта

```bash
# Использование скрипта
./create-new-miniapp.sh название-проекта
cd ../название-проекта
npm install

# Или ручное копирование
cp -r my-project новый-проект
cd новый-проект
rm -rf .git node_modules .next
git init
npm install
```

## 📋 Обязательные файлы

1. `minikit.config.ts` - конфигурация
2. `app/.well-known/farcaster.json/route.ts` - manifest endpoint
3. `app/layout.tsx` - с `generateMetadata`
4. `app/components/BaseMeta.tsx` - вызывает `sdk.actions.ready()`

## 🔑 Ключевые моменты

- **SDK ready()** - обязательно вызывать
- **Manifest структура** - `{ accountAssociation, miniapp }`
- **Embed metadata** - через `generateMetadata` с `other`
- **Action type** - `launch_miniapp` (не `launch_frame`)
- **base:app_id** - через `other` в metadata

## 📝 Шаблоны кода

### Manifest endpoint
```typescript
export async function GET() {
  return NextResponse.json({
    accountAssociation: minikitConfig.accountAssociation,
    miniapp: minikitConfig.miniapp,
  });
}
```

### BaseMeta
```typescript
useEffect(() => {
  sdk.actions.ready().catch(() => {});
}, []);
```

### generateMetadata
```typescript
other: {
  'fc:miniapp': JSON.stringify({
    version: 'next',
    imageUrl: '...',
    button: { action: { type: 'launch_miniapp', ... } }
  }),
  'base:app_id': '...'
}
```

## 🔄 Процесс деплоя

1. GitHub репозиторий
2. Vercel деплой
3. Обновить ROOT_URL
4. Отключить Deployment Protection
5. Создать accountAssociation на base.dev
6. Обновить accountAssociation
7. Опубликовать в Base app

## 🌐 Важные URL

- Manifest: `/.well-known/farcaster.json`
- Account association: `base.dev` (не base.org/build)
- Preview: `base.dev/preview`
- Vercel: `vercel.com`

## 📦 Зависимости

```json
{
  "dependencies": {
    "@farcaster/miniapp-sdk": "^0.2.1",
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
```

## ✅ Чеклист

- [ ] Проект создан
- [ ] Конфигурация обновлена
- [ ] Приложение создано
- [ ] Деплой на Vercel
- [ ] ROOT_URL обновлен
- [ ] Account association создан
- [ ] Manifest проверен
- [ ] Metadata проверена
- [ ] Опубликовано в Base app

---

**Быстрая справка для создания Base Mini Apps!**

