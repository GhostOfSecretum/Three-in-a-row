# 🧠 Памятка для создания новых Base Mini Apps

## 🎯 Быстрая справка

### Что нужно для нового проекта:

1. **Структура проекта** - скопировать из этого шаблона
2. **Зависимости** - `@farcaster/miniapp-sdk@0.2.1`
3. **Manifest endpoint** - `app/.well-known/farcaster.json/route.ts`
4. **Layout с metadata** - `app/layout.tsx` с `generateMetadata`
5. **BaseMeta компонент** - вызывает `sdk.actions.ready()`
6. **Конфигурация** - `minikit.config.ts`

## 📋 Пошаговый процесс (кратко)

### 1. Создание проекта
```bash
./create-new-miniapp.sh название-проекта
cd название-проекта
npm install
```

### 2. Настройка
- Обновить `minikit.config.ts`
- Создать приложение в `app/page.tsx`

### 3. Деплой
- GitHub репозиторий
- Vercel деплой
- Обновить ROOT_URL

### 4. Base интеграция
- Отключить Deployment Protection
- Создать accountAssociation на base.dev
- Обновить accountAssociation в конфигурации
- Опубликовать в Base app

## 🔑 Ключевые моменты

### Обязательные элементы:

1. **SDK ready()** - обязательно вызывать
2. **Manifest структура** - `{ accountAssociation, miniapp }`
3. **Embed metadata** - через `generateMetadata` с `other`
4. **Action type** - `launch_miniapp` (не `launch_frame`)
5. **base:app_id** - через `other` в metadata

### Важные детали:

- Manifest: `/.well-known/farcaster.json`
- Account association: создается на base.dev
- Deployment Protection: должен быть отключен
- Signature: может быть коротким (Farcaster) или длинным (Base)

## 📝 Шаблоны кода

### Manifest endpoint:
```typescript
export async function GET() {
  return NextResponse.json({
    accountAssociation: minikitConfig.accountAssociation,
    miniapp: minikitConfig.miniapp,
  });
}
```

### BaseMeta:
```typescript
useEffect(() => {
  sdk.actions.ready().catch(() => {});
}, []);
```

### generateMetadata:
```typescript
other: {
  'fc:miniapp': JSON.stringify({
    version: 'next',
    imageUrl: '...',
    button: {
      action: { type: 'launch_miniapp', ... }
    }
  }),
  'base:app_id': '...'
}
```

## 🚀 Команды

```bash
# Создание нового проекта
./create-new-miniapp.sh название

# Локальная разработка
npm run dev

# Сборка
npm run build

# Деплой (после настройки GitHub)
git push
```

## 📚 Документация

- Base Docs: https://docs.base.org/mini-apps
- Этот проект: полный шаблон готов к использованию

---

**Используйте этот проект как шаблон для новых Base Mini Apps!**

