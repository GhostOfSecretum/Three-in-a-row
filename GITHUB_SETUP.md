# 🐙 Создание GitHub репозитория

## ✅ Автоматический способ (если установлен GitHub CLI)

### Шаг 1: Установите GitHub CLI (если еще не установлен)

**macOS:**
```bash
brew install gh
```

**Другие системы:**
Следуйте инструкциям: https://cli.github.com/manual/installation

### Шаг 2: Авторизуйтесь в GitHub

```bash
gh auth login
```

Следуйте инструкциям на экране.

### Шаг 3: Создайте репозиторий автоматически

```bash
cd /Users/rustamahatov/.cursor/my-project
./create-github-repo.sh
```

Скрипт спросит:
- Название репозитория (например: `base-match3-game`)
- Видимость (Public или Private)

И автоматически создаст репозиторий и запушит код!

---

## 📝 Ручной способ (если нет GitHub CLI)

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [github.com/new](https://github.com/new)
2. **Repository name:** Введите название (например: `base-match3-game`)
3. **Description:** (опционально) "Игра Три в ряд для Base app"
4. **Visibility:** Выберите Public или Private
5. **НЕ добавляйте** README, .gitignore или лицензию (у нас уже все есть)
6. Нажмите **"Create repository"**

### Шаг 2: Подключите локальный репозиторий

```bash
cd /Users/rustamahatov/.cursor/my-project

# Замените YOUR_USERNAME и YOUR_REPO на ваши значения
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Запушьте код
git branch -M main
git push -u origin main
```

### Шаг 3: Проверьте

Откройте ваш репозиторий на GitHub - там должен быть весь код!

---

## 🚀 После создания репозитория

Следующий шаг - деплой на Vercel:

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New"** → **"Project"**
3. Импортируйте ваш GitHub репозиторий
4. Нажмите **"Deploy"**

Подробнее в файле `DEPLOY_NOW.md`

---

## 💡 Быстрая команда (если уже есть GitHub CLI)

```bash
cd /Users/rustamahatov/.cursor/my-project
gh repo create base-match3-game --public --source=. --remote=origin --push
```

Замените `base-match3-game` на желаемое название.

---

## ❓ Проблемы?

### "gh: command not found"
- Установите GitHub CLI: `brew install gh` (macOS)

### "Authentication required"
- Выполните: `gh auth login`

### "Repository already exists"
- Выберите другое название или удалите существующий репозиторий

### Ошибка при push
- Проверьте что вы авторизованы: `gh auth status`
- Проверьте URL репозитория: `git remote -v`

