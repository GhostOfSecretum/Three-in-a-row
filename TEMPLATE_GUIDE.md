# 🚀 Шаблон для создания Base Mini App

## 📋 Быстрый старт для нового проекта

Этот шаблон содержит все необходимое для быстрого создания нового Base Mini App.

## 🎯 Что включает этот шаблон

- ✅ Полная структура Next.js проекта
- ✅ Интеграция с Base app (manifest, embed metadata)
- ✅ SDK для Base Mini Apps
- ✅ Готовые компоненты и утилиты
- ✅ Скрипты для автоматизации
- ✅ Подробная документация

## 🚀 Создание нового проекта за 5 минут

### Шаг 1: Копирование шаблона

```bash
# Скопируйте проект
cp -r my-project новый-проект
cd новый-проект

# Или создайте новый проект на основе этого
```

### Шаг 2: Обновление конфигурации

1. Откройте `minikit.config.ts`
2. Обновите:
   - `ROOT_URL` - будет обновлен после деплоя
   - `name` - название вашего приложения
   - `description` - описание
   - Все остальные поля под ваше приложение

3. Обновите `package.json`:
   - `name` - название пакета
   - `description` - описание

### Шаг 3: Создание вашего приложения

Замените содержимое `app/page.tsx` на ваше приложение.

### Шаг 4: Деплой

Следуйте инструкциям в `DEPLOY_NOW.md`

## 📁 Структура шаблона

```
проект/
├── app/
│   ├── .well-known/farcaster.json/route.ts  # Manifest endpoint
│   ├── api/webhook/route.ts                  # Webhook endpoint
│   ├── components/BaseMeta.tsx               # SDK ready() и meta теги
│   ├── layout.tsx                            # Layout с generateMetadata
│   └── page.tsx                              # Ваше приложение
├── components/                               # Ваши компоненты
├── lib/                                      # Утилиты и логика
├── public/                                   # Статические файлы
├── minikit.config.ts                         # Конфигурация Base Mini App
├── package.json                              # Зависимости
└── Скрипты и документация
```

## 🔧 Ключевые файлы для копирования

### 1. Manifest endpoint
`app/.well-known/farcaster.json/route.ts` - всегда одинаковый, просто обновите импорт

### 2. Layout с metadata
`app/layout.tsx` - используйте `generateMetadata` для embed metadata

### 3. BaseMeta компонент
`app/components/BaseMeta.tsx` - вызывает `sdk.actions.ready()`

### 4. Конфигурация
`minikit.config.ts` - обновите под ваше приложение

## 📝 Чеклист для нового проекта

- [ ] Скопирован шаблон
- [ ] Обновлен `minikit.config.ts`
- [ ] Обновлен `package.json`
- [ ] Создано ваше приложение в `app/page.tsx`
- [ ] Созданы изображения (или использованы placeholder)
- [ ] Деплой на Vercel
- [ ] Обновлен ROOT_URL
- [ ] Создан accountAssociation
- [ ] Обновлен accountAssociation в конфигурации
- [ ] Проверено на base.dev/preview
- [ ] Опубликовано в Base app

## 🎨 Кастомизация

### Изменение стилей
- Обновите `app/globals.css`
- Обновите `tailwind.config.js`

### Добавление компонентов
- Создавайте в `components/`
- Импортируйте в `app/page.tsx`

### Добавление API роутов
- Создавайте в `app/api/`
- Используйте Next.js App Router формат

## 📚 Документация

Все инструкции сохранены в проекте:
- `DEPLOY_NOW.md` - Пошаговая инструкция деплоя
- `INTEGRATION.md` - Интеграция с Base app
- `README.md` - Основная документация

## 🎯 Быстрый старт команды

```bash
# 1. Скопировать шаблон
cp -r my-project новый-проект
cd новый-проект

# 2. Установить зависимости
npm install

# 3. Обновить конфигурацию
# Отредактировать minikit.config.ts

# 4. Создать приложение
# Отредактировать app/page.tsx

# 5. Локальная проверка
npm run dev

# 6. Деплой
# Следуйте DEPLOY_NOW.md
```

---

**Этот шаблон готов к использованию для создания новых Base Mini Apps!**

