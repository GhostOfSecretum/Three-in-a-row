# 🔧 Как обновить ROOT_URL в minikit.config.ts

## 📋 Что это такое?

`ROOT_URL` - это адрес вашего приложения после деплоя на Vercel. Его нужно обновить после того, как вы задеплоили проект.

## 🎯 Когда это делать?

**После шага 2** (деплой на Vercel), когда у вас уже есть URL вашего приложения.

Например: `https://your-app.vercel.app` или `https://base-match3-game.vercel.app`

---

## 📝 Пошаговая инструкция

### Шаг 1: Найдите URL вашего приложения

После деплоя на Vercel вы получите URL. Он будет выглядеть так:
- `https://your-app-name.vercel.app`
- Или кастомный домен, если вы его настроили

**Где найти:**
1. Откройте ваш проект на [vercel.com](https://vercel.com)
2. Вверху страницы будет URL вашего приложения
3. Скопируйте его (например: `https://base-match3-game.vercel.app`)

### Шаг 2: Откройте файл minikit.config.ts

**Способ 1: В редакторе (Cursor/VS Code)**
- Откройте файл `minikit.config.ts` в редакторе
- Или выполните: `code /Users/rustamahatov/.cursor/my-project/minikit.config.ts`

**Способ 2: В терминале**
```bash
cd /Users/rustamahatov/.cursor/my-project
open minikit.config.ts
```

### Шаг 3: Найдите строку с ROOT_URL

Найдите эту строку (обычно в начале файла):

```typescript
const ROOT_URL = process.env.NEXT_PUBLIC_ROOT_URL || 'http://localhost:3000';
```

### Шаг 4: Обновите ROOT_URL

**Вариант A: Замените на ваш URL напрямую**

Замените строку на:

```typescript
const ROOT_URL = 'https://your-app.vercel.app';
```

**Пример:**
Если ваш URL `https://base-match3-game.vercel.app`, то:

```typescript
const ROOT_URL = 'https://base-match3-game.vercel.app';
```

**Вариант B: Оставьте переменную окружения (рекомендуется)**

Оставьте как есть, но добавьте переменную окружения в Vercel:

1. В Vercel: Settings → Environment Variables
2. Добавьте:
   - **Name:** `NEXT_PUBLIC_ROOT_URL`
   - **Value:** `https://your-app.vercel.app`
3. Сохраните
4. Передеплойте проект

Тогда код останется:
```typescript
const ROOT_URL = process.env.NEXT_PUBLIC_ROOT_URL || 'http://localhost:3000';
```

---

## ✅ Пример полного файла после обновления

```typescript
// Конфигурация для Base Mini App
// Обновите ROOT_URL после деплоя на Vercel
const ROOT_URL = 'https://base-match3-game.vercel.app'; // ← ВАШ URL ЗДЕСЬ

export const minikitConfig = {
  accountAssociation: {
    "header": "",
    "payload": "",
    "signature": ""
  },
  miniapp: {
    version: "1",
    name: "Три в ряд", 
    subtitle: "Классическая игра-головоломка", 
    description: "Увлекательная игра три в ряд для Base app!...",
    screenshotUrls: [`${ROOT_URL}/screenshot-portrait.svg`],
    iconUrl: `${ROOT_URL}/icon.svg`,
    // ... остальное
  },
} as const;
```

---

## 🔄 После обновления

### 1. Сохраните файл

Нажмите `Cmd+S` (Mac) или `Ctrl+S` (Windows/Linux)

### 2. Запушьте изменения в GitHub

```bash
cd /Users/rustamahatov/.cursor/my-project

# Добавьте изменения
git add minikit.config.ts

# Создайте коммит
git commit -m "Update ROOT_URL to production URL"

# Запушьте
git push
```

### 3. Vercel автоматически передеплоит

Vercel автоматически обнаружит изменения и передеплоит проект.

Или вручную:
- Откройте проект в Vercel
- Нажмите "Redeploy"

---

## 🎯 Проверка

После обновления проверьте:

1. **Manifest доступен:**
   - Откройте: `https://your-app.vercel.app/.well-known/farcaster.json`
   - Должен вернуться JSON с вашими данными

2. **Все URL правильные:**
   - Проверьте что все пути начинаются с вашего URL
   - Не должно быть `localhost:3000` в продакшене

---

## ❓ Частые вопросы

### Как узнать мой URL на Vercel?

1. Откройте [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. URL будет вверху страницы

### Нужно ли обновлять каждый раз?

Нет, только один раз после первого деплоя. Если вы меняете домен, обновите снова.

### Что если я использую кастомный домен?

Просто замените URL на ваш кастомный домен:
```typescript
const ROOT_URL = 'https://mygame.com';
```

### Можно ли оставить localhost?

Нет, для Base app нужен публичный URL. `localhost` работает только локально.

---

## 🎉 Готово!

После обновления ROOT_URL переходите к следующему шагу - настройке Base app (см. DEPLOY_NOW.md, шаг 4).

